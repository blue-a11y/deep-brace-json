# DeepBrace JSON 开发规划

> 本文档只记录未实现、进行中或待验收的工作。当前顶层能力见双语 README，具体行为由源码和测试
> 验证，历史开发流水以 Git 为准。

## 规划规则

- 每个工作项使用稳定规划 ID，并清楚描述范围、目标与验收标准。
- “完成”必须同时满足代码、自动化检查、必要的浏览器验收和文档联动，不能只以代码写完或测试通过判断。
- 完成后的工作项从当前规划移除；如果改变 README 顶层能力，同时同步英文和简体中文 README。
- 规划状态只使用：`待决策`、`待开始`、`进行中`、`验收中`、`已阻塞`。完成项从当前规划移除，结果由源码、测试、必要的 README 摘要和 Git 保存。
- 候选需求不代表已经承诺开发；进入当前规划前必须补齐范围与验收标准。

## 当前阶段目标

当前阶段聚焦“本地数据可靠性与可验证性”：完成 IndexedDB v3 结构化持久化交付，修正与实际行为冲突的产品语义，并为解析、标签、响应式和重置链路建立可重复的回归基线。

## 当前规划

### `PLAN-001` IndexedDB v3 持久化整体验收

- **状态**：验收中。
- **优先级**：P0。
- **目标**：以三个无 `keyPath` 的 Object Store 保存应用状态、分栏和滚动，确保启动恢复、重置、撤销和刷新之间没有竞态。
- **自动化验收**：
  - v2 打开 v3 后重建三个 Store，且均为 out-of-line key。
  - v2 fixture 与真实上一版 schema 一致，向三个旧 Store 写入探针数据；升级后断言 Store 已重建且
    旧数据均未保留，明确证明当前“不迁移”的清空策略。
  - 应用状态 record root 不重复保存 Object Store primary key 或额外 `state` 包装；标签业务 ID
    正常保留，structured clone 类型不丢失。
  - 滚动使用 `[tabId, area]` 复合外部键，分栏在 IndexedDB 中保存对象。
  - 重置和撤销在 React 重挂载前同步切换滚动与布局内存缓存。
  - 重置 / 撤销的完成 Promise 覆盖会影响立即刷新结果的应用状态、滚动和布局写入；不得只有滚动
    adapter 提供写入屏障。
  - IndexedDB 用例显式隔离数据库与模块缓存，不依赖执行顺序，并覆盖失败、连续写和重置 / 撤销
    交错。
- **浏览器验收**：构造多标签、暗色、非默认代码字体、8 空格、自定义主题、关闭长字符串、80/20 分栏和双区非零滚动；验证重置为默认、8 秒内撤销、刷新后仍保持撤销结果。
- **当前自动化进度**：已覆盖多标签、代码字体、自定义主题、长字符串偏好、非默认分栏、双区非零滚动、app-state 显式
  写入屏障、滚动 / 布局重置恢复事务、重置 / 撤销 action Promise 对 app-state 屏障的等待，以及
  撤销完成后立即刷新恢复；暗色、8 空格和精确 80/20 边界仍待补齐。
- **边界**：按当前产品决定，不迁移旧 localStorage 或未发布的 v1 / v2 IndexedDB 数据。

### `PLAN-002` 统一重置文案与 Modal 动作约定

- **状态**：待开始。
- **优先级**：P0。
- **现状**：确认弹窗写“此操作不可撤销”，实际重置后 8 秒内可以完整撤销；设置 Modal 还使用 Footer 承载即时危险操作，需要与根 `AGENTS.md` 的 Modal 约定统一解释或调整结构。
- **验收标准**：
  - 确认文案准确说明重置范围和 8 秒撤销窗口。
  - 设置 Modal 不出现仅用于关闭的 Footer 或“完成”按钮。
  - 危险操作的入口、二次确认、关闭行为和键盘焦点符合统一 Modal 约定。
  - 如果改变 README 中的顶层能力描述，同步更新英文和简体中文版本。

### `PLAN-003` 明确并统一“全局缩进”作用范围

- **状态**：待决策。
- **优先级**：P0。
- **现状**：2 / 4 / 8 空格设置会影响树几何和复制序列化，但编辑器“格式化”固定使用 2 空格；“全局缩进”名称容易产生会影响格式化的预期。
- **决策选项**：
  1. 让格式化读取全局缩进，保持“全局”语义。
  2. 保持格式化固定 2 空格，并把设置名称和说明收敛为“树与复制缩进”。
- **验收标准**：选定一种语义后，按钮提示、设置名称、实现、测试和 README 摘要完全一致；2 / 4 / 8 三档均验证。

### `PLAN-005` 补齐核心纯逻辑自动化测试

- **状态**：进行中。
- **优先级**：P1。
- **范围**：
  - JSON、JSON5、纯文本降级、非法 JSON 行列提取。
  - 格式化、压缩、多层转义、字符串字面量和裸转义容器的逐层反转义。
  - 新建、插入、关闭、最后一个标签保护、撤销位置、前后循环切换。
  - 快捷键组合匹配、重复按键、IME、Ctrl / Meta 冲突的纯逻辑用例已覆盖；弹层保护由浏览器用例覆盖。
    标准 JSON 快路径、JSON5 兜底与错误行列、文本降级、嵌套容器检测和大纲行数缓存已有用例；
    其余转换和标签纯逻辑边界仍待补齐。
- **验收标准**：边界用例按解析、转换、标签和快捷键领域清楚组织；测试不依赖脆弱的实现细节；`pnpm test` 可稳定重复运行。

### `PLAN-006` 建立组件与浏览器回归矩阵

- **状态**：进行中。
- **优先级**：P1。
- **范围**：
  - 已建立 Playwright + Chromium 基线，覆盖首屏健康、JSON5 与嵌套解析、偏好和双区滚动刷新
    恢复、多标签重置 / 撤销与分栏恢复，以及窄屏工具菜单；测试使用独立 4173 服务和
    BrowserContext。
  - 长字符串在 120 / 121 字符边界的截断、全文弹层高度与滚动。
  - 树单节点和全部折叠、稳定行号、换行 / 横向滚动、逐节点和整树复制。
  - 339/340、479/480、559/560、599/600、679/680、767/768、799/800、859/860px 关键宽度。
  - 工具菜单与直显动作功能等价，图标和 Logo 不压缩、不换行。
  - 编辑器 / 树滚动、分栏拖拽、刷新、移动端切换、重置和撤销。
- **已覆盖边界**：单个非活动标签、活动标签与右侧批量关闭撤销后双区滚动继续落库及刷新恢复；
  输入编辑后在自动解析前刷新不继承旧树折叠状态，以及未修改内容的节点折叠刷新恢复。
  快捷键新增编辑器聚焦保留选区、重命名、左右插入与批量关闭撤销；统一修饰键的即时提示、刷新、
  重置和撤销恢复；桌面与窄屏弹层入口、菜单和输入框保护。系统保留键及 Firefox / WebKit 仍待验证。
  大文档输入期间不重复序列化旧树、重新解析后的折叠与末尾数据可达，以及根节点类型连续切换已有用例。
  真实剪贴板粘贴约 1 MB / 10 MB、后台解析、虚拟树 DOM 数量限制、滚动到末尾、长值预览与完整
  复制已有用例；新增动态行高、标签切换与刷新锚点、字体 / 换行 / 视口变化及键盘折叠回归。
  请求替换、关闭与取消、后台恢复期间折叠状态保留已有单元测试，后续需评估更大输入的内存预算。
  示例菜单的普通 / 约 1 MB / 约 10 MB 数据、桌面 / 窄屏等价入口、键盘选择与刷新恢复已有回归；
  后台生成失败保留内容、任务替换、编辑取消和切换 / 关闭标签已有单元测试。
  大纲行号列随位数扩展，覆盖四位数不换行；800 条记录（紧凑文本约 11.5 万字符）的文档刷新时
  仍会触发 CodeMirror 的 `Measure loop restarted more than 5 times` 警告，格式化后也可复现，
  需继续定位编辑器布局与滚动恢复边界。
- **验收标准**：形成可重复执行的组件测试或浏览器脚本；不能自动化的视觉项保留明确手工步骤与预期结果。

### `PLAN-007` 清理状态生命周期边界

- **状态**：待开始。
- **优先级**：P2。
- **现状**：关闭标签后，对应的 IndexedDB 滚动记录不会立即删除；多个页面之间也不会实时同步状态。
- **验收标准**：决定孤立滚动记录的清理时机并覆盖撤销关闭场景；明确多页面并发是支持、提示还是不处理，避免无界增长和误覆盖。

### `PLAN-008` 建立生产包体与首屏性能预算

- **状态**：进行中。
- **优先级**：P2。
- **现状**：品牌动效延迟加载及静态降级、非阻塞字体和 HTML 响应式骨架屏已实现。
  CodeMirror 与设置、主题、快捷键弹窗保持直接加载，打开弹窗不依赖额外脚本下载；
  HeroUI 弹窗外壳保留焦点上下文，并覆盖桌面与窄屏首次打开、关闭和快捷键重开。
  已补充启动与动态资源慢加载、失败回归，以及骨架到首帧和品牌动效接入的逐帧布局回归。
  已根据线上发现补充非阻塞 CSS 下载、挂载前样式屏障及生产慢加载、失败重试回归。
  主 JavaScript chunk 仍超过 Vite 默认 500kB 提示阈值；
  还需定义固定环境下的包体、首屏加载和交互就绪预算并接入 CI，并扩充线上多地区冷 / 热缓存采样。
  线上已确认骨架不再等待主 CSS；域名已切回 Vercel DNS 并直连 Vercel，网络传输波动仍需独立排查，
  不以单次测速或 Vercel 缓存命中代表整条链路性能。
- **验收标准**：先建立可重复的产物大小和首屏指标，再决定代码分割或依赖拆分策略；优化后不得以隐藏警告代替指标改善，并记录对 CodeMirror、HeroUI 和主题资源加载的影响。

### `PLAN-009` 将项目开发规范转为可执行门禁

- **状态**：进行中。
- **优先级**：P1。
- **关联文档**：[docs/DEVELOPMENT.md](DEVELOPMENT.md)。
- **现状**：文件命名、部分标识符和格式已有脚本检查，但依赖方向、完整组件声明、Hooks、a11y 和
  双语 README 结构仍主要依赖人工审查；真实浏览器验收已建立 Playwright 基线，
  但完整回归矩阵与 CI 尚未完成；Playwright E2E 已启用 strict，应用与 Node 构建配置尚未启用。
- **验收标准**：
  - 提供统一 `pnpm check` 入口，执行测试、静态检查、类型 / 构建和文档一致性检查。
  - 自动检查双语 README 章节、内部文档链接、规划 ID 格式以及必要的文档联动。
  - 补齐 React、Hooks 和 a11y lint 规则，并让组件声明检查覆盖匿名包装、`memo` 等常见形式。
  - 评估并分阶段为应用与 Node 构建配置启用 TypeScript strict；每阶段不通过类型断言掩盖错误。
  - 决定并固定受支持的 Node 与 pnpm 版本，在干净环境验证 frozen lockfile 安装。
  - 将 Playwright 接入 CI，保留失败 trace / 截图，并持续明确自动化项和仍需手工验收项。

## 候选需求

以下方向尚未承诺排期，进入当前规划前需要单独确认：

| 候选 ID        | 方向                     | 需要先回答的问题                                                                                                         |
| -------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| `CANDIDATE-01` | 存储使用量与失败提示     | 是否在设置中展示 `navigator.storage.estimate()` 的 Origin 级使用量；配额错误是否需要用户可见反馈，而不是仅控制台警告。   |
| `CANDIDATE-02` | 大型 JSON 首次渲染与内存 | 在后台解析与树虚拟化基础上，定义更大输入的内存预算；展开行模型和尺寸元数据仍随节点数增长，需评估增量索引与分段滚动高度。 |
| `CANDIDATE-03` | 无障碍专项               | 是否建立键盘遍历、焦点恢复、读屏语义、对比度和 reduced-motion 的完整验收基线。                                           |

## 明确暂不纳入

- 不迁移旧 localStorage、IndexedDB v1 或未发布的 v2 数据；升级到 v3 可以清空旧本地状态。
- 不增加云同步、账号系统或把用户 JSON 上传到服务端的能力。
- 不把提交批次、排障流水或已经完成的功能长期堆放在本规划中。

## 质量门禁

完整规则和自动检查边界见 [docs/DEVELOPMENT.md](DEVELOPMENT.md)。

### 自动化检查

每个功能批次至少执行：

```bash
pnpm test
pnpm lint
pnpm build
git diff --check
```

根据变更范围增加针对性测试；已有测试通过不等于未覆盖的浏览器交互已经验证。

### 浏览器验收基线

涉及对应功能时至少覆盖：

1. JSON、JSON5、普通文本和非法 JSON。
2. 格式化、压缩、连续转义 / 反转义，以及嵌套 `Parse`。
3. 多标签的新建、切换、关闭、撤销关闭和刷新恢复。
4. 编辑器 / 树滚动位置、50/50 与自定义分栏比例。
5. 设置、长字符串 120 / 121 边界、重置、8 秒撤销和再次刷新。
6. 工具栏关键断点、溢出菜单等价性和 767 / 768px 布局切换。
7. CodeMirror、Modal、Select、Dropdown 聚焦时的快捷键行为。

### 文档联动

完成前逐项确认：

- 当前行为、默认值、边界和持久化范围已有对应测试或浏览器验收证据。
- 顶层能力、技术栈、脚本或启动命令变化时，两版 README 的结构和内容同步。
- 当前规划状态已更新；完成项不继续留在“当前规划”。
- 专项文档中的“当前状态”没有与源码产生冲突。

## 完成流转

```text
规划项明确范围与验收
  → 实现并补测试
  → 自动化检查
  → 按功能范围完成浏览器验收
  → 更新必要的双语 README 与专项文档
  → 从 docs/ROADMAP.md 当前规划移除
  → 由 Git 保存交付历史
```
